# bagian a
set.seed(1)
l = 3
#dexp(x = seq(1, 10, by = 0.1) ,)
rexp(10)

# bagian b
hist(rexp(10))
hist(rexp(100))
hist(rexp(1000))
hist(rexp(10000))

# bagian c
# mean
1 / l

# variance
1 / (l * l)
